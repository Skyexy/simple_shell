#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
#include <string.h>
#include <sys/wait.h>

int _strlen(char *s);
char *_strncat(char *dest, char *src, int n);
char *_strdunp(char *lo);

#endif /* MAIN_H */
